services:
  wizard:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app/app
      - ./app/templates:/app/app/templates
      - ./data:/app/data
    environment:
      - TZ=America/Recife
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload

